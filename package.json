{
  "name": "opensphinx-workspace",
  "private": true,
  "packageManager": "pnpm@9.9.0",
  "engines": {
    "node": ">=18.18"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --filter=opensphinx-demo",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.9.6"
  }
}
